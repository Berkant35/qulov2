---
name: run-server
description: Start the Qulo backend server in dev mode
disable-model-invocation: true
---

## Steps

1. Navigate to the server directory
2. Run the dev server with tsx watch

```bash
cd /Users/berkantcalikusu/IdeaProjects/qulov2/server && npm run dev
```

The server runs on the port defined in `.env` (default: 3000).
